import React from 'react';
import {SafeAreaView} from 'react-native';

import {SectionHeader} from 'component/partial';

const Stay: React.FC = () => {
  return (
    <SafeAreaView>
      <SectionHeader heading="Stay" />
    </SafeAreaView>
  );
};

export default Stay;
