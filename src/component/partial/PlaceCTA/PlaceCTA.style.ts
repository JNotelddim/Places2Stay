import {colors, spacing} from 'const';
import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  locationText: {
    color: colors.slateGrey,
    marginTop: spacing.whitespace.small,
  },
});
export default styles;
